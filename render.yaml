services:
  - type: web
    name: pullup-calculator
    runtime: python
    plan: free
    buildCommand: pip install uv && uv sync
    startCommand: uv run gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SECRET_KEY
        generateValue: true
      - key: TIMELINE_PASSWORD
        sync: false
      - key: PYTHON_VERSION
        value: 3.13.0
